# 本番審査システム（/v2）機能概要

事前審査システム（`/`）とは独立した、本番審査用のスコアリング・順位発表システムです。
既存データ（`/config`・`/scores`・`/activeTeam`）は**読み取り専用**で参照し、書き込みは新設ノード `/v2` 配下のみで行うため、既存データには一切影響しません。

---

## 1. 画面構成とパス

| パス | ファイル | 役割 |
| :--- | :--- | :--- |
| `/v2` | `public/v2/index.html` | トップ（審査員選択・管理者/発表への導線） |
| `/v2/judge?id=<judgeId>` | `public/v2/judge/index.html` | 審査員の採点画面（上位12アプリ一覧＋採点フォーム） |
| `/v2/admin` | `public/v2/admin/index.html` | 管理者画面（審査員管理・リアルタイム順位・進捗） |
| `/v2/display` | `public/v2/display/index.html` | 結果発表画面（会場スクリーン用） |

ローカル開発では `server.js` に `/v2` 系ルートを追加済み。Cloudflare へは静的アセットとして配信され、`npx wrangler deploy` でそのまま公開できます。

---

## 2. 対象アプリ（上位12）の抽出ロジック

- 事前審査のスコア（`/scores`）とアプリ情報（`/config/teams`）を**読み取り**
- 各アプリの合計点（審査員 `total` の合算）を集計し、降順ソートして**上位12件**を対象とする
- データの移動・コピーは行わず、常に参照して算出（純粋リファレンス方式）

---

## 3. 採点仕様

### 評価項目（4項目・各1〜5点・重み付き）

| 項目キー | 項目名 | 配点 |
| :--- | :--- | :--- |
| `problemSetting` | 課題設定 | 30% |
| `solutionIdea` | 解決アイデア | 30% |
| `practicality` | 実用性・完成度 | 25% |
| `presentation` | プレゼン・伝え方 | 15% |

### スコア計算（100点満点換算）

$$\text{totalScore} = \sum \left( \frac{\text{入力点}(1\sim5)}{5} \times \text{配点(\%)} \right)$$

- コメント入力（任意）あり
- 全項目を選択しないと送信不可
- 送信後は自動でアプリ一覧へ戻り、「✓ 点数を送信しました」トーストを表示
- 採点済みアプリは再選択で修正可能

---

## 4. 審査員フロー

1. 管理者が発行した専用URL（`/v2/judge?id=<judgeId>`）からアクセス
2. 上位12アプリ一覧を表示（順位バッジ・採点済み/未採点ステータス付き）
3. アプリを選択して4項目を採点＋コメント入力
4. 送信 → 一覧へ自動リダイレクト＋送信完了トースト

※ 専用URL未設定の場合は案内画面を表示（誤操作・成りすまし防止のため自由な切り替えは無効）。

---

## 5. 管理者画面の機能

- **審査員管理**: 追加・名称編集・削除、専用URLのワンクリックコピー
- **総合順位（リアルタイム）**: 審査員 `totalScore` の**平均（100点満点）**で順位付け（合計スコアも併記）
- **採点進捗マトリックス**: 審査員×アプリの採点済み/未採点を一覧表示

---

## 6. 結果発表画面（/v2/display）

- 会場スクリーン向けのランキング表示
- **審査員全員の合計スコア順**で順位付け（1〜3位は金/銀/銅で装飾）
- Firebase のリアルタイム更新に追従

---

## 7. Firebase データ構造（`/v2` 配下）

```json
{
  "v2": {
    "config": {
      "judges": {
        "<judgeId>": { "name": "本番審査員1", "order": 1720000000 }
      }
    },
    "scores": {
      "<teamId>": {
        "<judgeId>": {
          "judgeId": "<judgeId>",
          "rawScores": {
            "problemSetting": 4,
            "solutionIdea": 3,
            "practicality": 5,
            "presentation": 4
          },
          "totalScore": 79.0,
          "comment": "コメント本文",
          "updatedAt": 1720000000
        }
      }
    }
  }
}
```

- 採点は `judgeId` 単位で保存され、上書きで修正可能
- 既存ノード（`/config`・`/scores` 等）は参照のみで、書き込みは `/v2` 配下に限定

---

## 8. 集計基準まとめ

| 画面 | 順位付けの基準 |
| :--- | :--- |
| 管理者画面（`/v2/admin`） | 審査員 `totalScore` の**平均**（100点満点） |
| 結果発表画面（`/v2/display`） | 審査員 `totalScore` の**合計** |
